🧩 Basic Level

Count vowels and consonants in a string.

Reverse a string without using built-in functions.

Check if a string is a palindrome (reads same backward and forward).

Convert lowercase letters to uppercase (and vice versa).

Find the length of a string without using .length() or .size().

Count the number of words in a sentence.

Remove spaces from a string.

Find the ASCII value of each character in a string.

Replace all spaces with ‘_’ (underscore).

Compare two strings without using == operator.


🔠 Intermediate Level

Find the frequency of each character in a string.

Find the first non-repeating character in a string.

Check if two strings are anagrams of each other.

Remove all duplicate characters from a string.

Find all substrings of a given string.

Count occurrences of a substring in a string.

Check if one string is a rotation of another (e.g., "abcde" → "cdeab").

Find the largest and smallest word in a sentence.

Reverse each word in a given sentence while keeping word order.

Check if a string contains only digits or alphabets.


🧠 Advanced Level

Implement your own stoi() (convert string to integer).

Find the longest substring without repeating characters.

Check if two strings are isomorphic.

Find the longest palindromic substring.

Implement pattern matching (like find or strstr() manually).

Find the smallest window in a string that contains all characters of another string.

Check if a string can be rearranged to form a palindrome.

Generate all permutations of a given string.

Implement run-length encoding (e.g., “aaabb” → “a3b2”).

Check if a given string follows a pattern (like “abba” → “dog cat cat dog”).